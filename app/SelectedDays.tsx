// SelectedDays.ts

// This will hold the selected days
export let selectedDays: string[] = []

// Function to update the selected days
export function setSelectedDays(days: string[]) {
  selectedDays = days
}
